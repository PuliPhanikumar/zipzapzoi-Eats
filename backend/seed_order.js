const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const rest = await prisma.restaurant.findFirst(); const user = await prisma.user.findFirst(); if (!rest) { console.log('No restaurants found.'); return; } await prisma.order.create({ data: { zoiId: 'ORD-999', restaurantId: rest.id, userId: user ? user.id : null, total: 400.0, status: 'PENDING', items: [{id: 102, name: 'Paneer Butter Masala', price: 280, quantity: 1}], deliveryAddress: 'Connaught Place', zone: 'New Delhi' } }); console.log('Order seeded!'); } main().catch(console.error).finally(() => prisma.$disconnect());
